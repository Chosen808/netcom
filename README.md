Netcom is a simple application built to support several methods of network communication.

Currently netcom only supports Linux systems. To build ensure the GNU Compiler Collection in installed and run make.
